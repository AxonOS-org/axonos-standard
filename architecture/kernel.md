# Kernel

The AxonOS kernel substrate is responsible for deterministic scheduling, bounded IPC, monotonic time, and enforcement points for capability-gated neural data flow.

Reference implementation:

- `axonos-kernel`
