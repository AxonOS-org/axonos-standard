# Architecture Overview

AxonOS separates neural hardware, deterministic processing, and application behavior.

The architecture is built around four boundaries:

1. acquisition boundary;
2. real-time processing boundary;
3. capability / neural permission boundary;
4. application boundary.

Raw neural data should not be treated as ordinary application data.
